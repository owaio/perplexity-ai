# Perplexity AI Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SOCKS Proxy Configuration (Optional)
# =============================================================================
# Format: socks5://[user[:pass]@]host[:port]
# Examples:
#   SOCKS_PROXY=socks5://127.0.0.1:1080
#   SOCKS_PROXY=socks5://user:pass@127.0.0.1:1080
# SOCKS_PROXY=

# =============================================================================
# Token Pool Configuration (Optional)
# =============================================================================
# Path to JSON config file containing multiple tokens for load balancing
# Format: {"tokens": [{"id": "user1", "csrf_token": "xxx", "session_token": "yyy"}, ...]}
# PPLX_TOKEN_POOL_CONFIG=./token_pool_config.json

# =============================================================================
# Admin Authentication (Required for pool management)
# =============================================================================
# Set this to enable admin authentication for the pool management web UI
# Without this, admin operations (add/remove/enable/disable tokens) will be disabled
# Generate a secure random token, e.g.: openssl rand -hex 32
PPLX_ADMIN_TOKEN=YTbb#2148
